import { Default } from 'components/layouts/Default';
import { Text } from '@chakra-ui/react';

const Roadmap = () => {
  return (
    <Default pageName="CB Radar">
      <Text>Roadmap goes here.</Text>
    </Default>
  );
};

export default Roadmap;
