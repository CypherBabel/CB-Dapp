import { Default } from 'components/layouts/Default';
import { Text } from '@chakra-ui/react';

const CBRadar = () => {
  return (
    <Default pageName="CB Radar">
      <Text>CB Radar goes here.</Text>
    </Default>
  );
};

export default CBRadar;
